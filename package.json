{
  "name": "monorepo-replit-api-server",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @workspace/hogy-indonesia dev",
    "build": "pnpm --filter @workspace/hogy-indonesia build",
    "start": "pnpm --filter @workspace/hogy-indonesia serve"
  },
  "dependencies": {
    "framer-motion": "^11.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
